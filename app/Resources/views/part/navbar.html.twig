 <!-- Fixed navbar -->
    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
            <div class="navbar-brand" href="#">Molecules IT <span class="glyphicon glyphicon-tint"></span></div>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li class="active"><a href="/">Home</a></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Chemicals<span class="caret"></span></a>
              <ul class="dropdown-menu">
                <!-- Proper way of displaying urls -->
                <li><a href="{{ path('chemicals_elements_list') }}">Elements</a></li>
                <li><a href="{{ path('chemicals_atoms_list') }}">Atoms</a></li>
                <li><a href="{{ path('chemicals_molecules_list') }}">Molecules</a></li>
              </ul>
            </li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Analysis<span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><a href="{{ path('chemicals_db_analysis') }}">Database Structure</a></li>
              </ul>
            </li>
            <li class="bg-primary">
                {% if is_granted('ROLE_USER') %}
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-user"> </span><span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        {% if is_granted('ROLE_ADMIN') %}
                            <li><a href="{{ path('app_users_list') }}">Manage users</a></li>
                        {% endif %}
                        <li><a href="{{ path('fos_user_profile_edit') }}">My profile - {{ app.user.username }}</a></li>
                        <li><a href="{{ path('fos_user_security_logout') }}"> <span class="glyphicon glyphicon-off"> </span> Logout</a></li>
                    </ul>
                {% else %}
                    <a href="{{ path('fos_user_security_login') }}"><span class="glyphicon glyphicon-user"> </span></a>
                {% endif %}
            </li>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>